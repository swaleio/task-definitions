# Normalize all text to LF in the repo; shell scripts and Dockerfiles must stay
# LF so container builds don't hit CRLF "bad interpreter" errors.
* text=auto eol=lf
*.sh text eol=lf
Dockerfile text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.py text eol=lf
